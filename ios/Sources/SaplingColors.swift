import SwiftUI
import UIKit

enum SaplingColors {
    // Primary brand — forest green, the color of a sapling
    static let brand = Color(red: 0.25, green: 0.45, blue: 0.32)
    static let brandUI = UIColor(red: 0.25, green: 0.45, blue: 0.32, alpha: 1)

    // Trail line — warm terracotta/clay, like a worn dirt path
    static let trail = Color(red: 0.69, green: 0.47, blue: 0.34)
    static let trailUI = UIColor(red: 0.69, green: 0.47, blue: 0.34, alpha: 1)

    // Recording states
    static let recording = Color(red: 0.25, green: 0.45, blue: 0.32)
    static let stopRecording = Color(red: 0.76, green: 0.31, blue: 0.27)

    // Water — mountain stream blue
    static let water = Color(red: 0.33, green: 0.55, blue: 0.65)
    static let waterUI = UIColor(red: 0.33, green: 0.55, blue: 0.65, alpha: 1)

    // Camp — campfire ember brown
    static let camp = Color(red: 0.62, green: 0.36, blue: 0.18)
    static let campUI = UIColor(red: 0.62, green: 0.36, blue: 0.18, alpha: 1)

    // Beauty — golden hour amber
    static let beauty = Color(red: 0.78, green: 0.55, blue: 0.15)
    static let beautyUI = UIColor(red: 0.78, green: 0.55, blue: 0.15, alpha: 1)

    // Service — weathered slate blue
    static let service = Color(red: 0.35, green: 0.52, blue: 0.58)
    static let serviceUI = UIColor(red: 0.35, green: 0.52, blue: 0.58, alpha: 1)

    // Custom — driftwood warm gray
    static let custom = Color(red: 0.58, green: 0.53, blue: 0.48)
    static let customUI = UIColor(red: 0.58, green: 0.53, blue: 0.48, alpha: 1)
}
