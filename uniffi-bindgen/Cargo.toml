[package]
name = "uniffi-bindgen"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "uniffi-bindgen"
path = "src/main.rs"

[dependencies]
# Must match the uniffi version used in sapling-ffi
uniffi = { version = "0.29", features = ["cli"] }
